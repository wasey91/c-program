#include<stdio.h>
int main(){
    int n = 5961, sum = 0,  rem;
    while(n>0){
        rem = n%10;
        sum = sum + rem;
        n = n/10;
    }
    printf("sum of number= %d" ,sum);
    return 0;
}
